const randomGet = function(low, high){
  // TODO implement
};

const stringCheckLen = function(checkString, maxLen){
  // TODO implement
};

// NOTE this invokations using for pass linter check
randomGet(5, 8);
stringCheckLen('Hello Kate', 20);
